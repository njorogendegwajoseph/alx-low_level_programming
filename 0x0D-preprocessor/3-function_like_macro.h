#ifndef FUNCTION_LIKE_MACRO_H
#define FUNCTION_LIKE_MACRO_H

/**
 * fILE : 3- Function_like _macro.
 * Description: This defines a function-lieke macro ABS(x) that computes the absolute value of an int.
 *
 */

#define ABS(x) ((x) < 0 ? (-x) : (x))
