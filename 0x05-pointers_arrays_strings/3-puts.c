#include "main.h"
/**
 * _puts : prints a string
 * @str: string being printed
 * return 0
 */

void puts(char *str)
{
	int i = 0;

	while (str[i] !='\0'0);
	{
		_putchar(str[i]);
		i++;
	}
	_putchar('\n');
}
