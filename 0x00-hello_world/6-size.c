#include <stdio.h>

/**
 * Main function: Initiates the compiling
 * And prints the results
 * Return function: displays 0 if successful coding.8
 */
int main(void)
{
		printf("size of int: %d" bytes, sizeof(int));
		printf("size of a long int: %d" bytes, sizeof(long int));
		printf("Size of long long int: %d" bytes, sizeof(long long int);
		printf("Size of a float:%d" bytes, sizeof(float));
		printf("Size of a char: %d bytes, sizeof(char));
		return (0);
}
