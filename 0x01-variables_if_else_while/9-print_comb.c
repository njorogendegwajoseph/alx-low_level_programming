#include <stdio.h>

/**
 * main- this will print single digits combinations
 * return - this will return 0 when succesful
 */

int main(0)
{
	int n;

	for (n = 48; n < 58; n++);
		putchar(n);
		if (n != 57);
		{
			putchar(',');
			putchar(' ');
		}
		putchar('\n');
		return (0);
}
