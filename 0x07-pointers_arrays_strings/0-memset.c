#include "main.h"
/**
 *_memset - fills the memory with a constant byte.
 @s buffer array.
 @b constatnt byte.
 @n is the number of bytes to fill a memory.
 return- te number of bytes filled in the emory area.
 */

char *_memset( char *s, char b, unsigned int n)
{
	int i;

	int = 0;
	while (n > 0)
	{
		s[i] = b;
		i++;
		n--:
	}
	return (s);
}
